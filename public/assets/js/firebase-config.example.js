// Copy this file to firebase-config.js (same folder) and fill in real
// values from the Firebase console (see /firebase/SETUP.md, steps 2-3).
// firebase-config.js is gitignored — never commit real keys here.
// While this file doesn't exist (or is left with placeholders),
// notifications.js quietly skips push-notification registration.

window.NITHI_FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};

window.NITHI_FIREBASE_VAPID_KEY = "REPLACE_ME";
